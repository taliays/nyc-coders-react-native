import React from 'react';
import { Text } from 'react-native';
import { Center } from '../components/Center';

export const Wallet = () => {
  return (
    <Center>
      <Text>Wallet</Text>
    </Center>
  ) 
}